routes:

collections:
  /public-goods/:
    permalink: /public-goods/{slug}/
    template: index
    filter: tag:public-goods
  /conversations/:
    permalink: /conversations/{slug}/
    template: conversations
    filter: tag:conversations
  /updates/:
    permalink: /updates/{slug}/
    template: updates
    filter: tag:updates
  /media/:
    permalink: /{slug}/
    template: media
    filter: tag:media
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
